def computepay(h, r):
    print ("In computepay", h, r)

    if h > 40:

        otp=(h-40)*r*1.5
        pay=(r*40) + otp
    #print ("Total pay: ", pay)
    else:
        pay=r*40
    return pay

hours = input("Enter Hours:")
payrate= input ("Enter PayRate:")
hrs=float (hours)
rph=float (payrate)

xp=computepay (hrs, rph)

print ("Total pay: ", xp)
